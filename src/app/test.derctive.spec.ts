import { TestDerctive } from './test.derctive';

describe('TestDerctive', () => {
  it('should create an instance', () => {
    const directive = new TestDerctive();
    expect(directive).toBeTruthy();
  });
});
