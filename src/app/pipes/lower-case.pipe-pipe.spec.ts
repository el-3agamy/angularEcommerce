import { LowerCasePipePipe } from './lower-case.pipe-pipe';

describe('LowerCasePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LowerCasePipePipe();
    expect(pipe).toBeTruthy();
  });
});
